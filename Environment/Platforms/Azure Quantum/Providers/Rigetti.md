#SoftDev 
**Rigetti** - superconducting QPUs. Billing is by **job exec time** only (no per-shot or per-gate charge). Includes free open-source QVM simulator.
## Targets
`rigetti.sim.qvm` (free open-source QVM), `rigetti.qpu.ankaa-9q-3` & newer Ankaa series. Billing per **QPU exec second** - no per-shot or per-gate charge. Input format: **Quil** (`rigetti.quil.v1`). Ankaa series uses $2D$ square lattice layout with tunable couplers for improved $2$-qubit gate fidelity.

QPU charged per second of exec time only. QVM simulator is free. No per-shot or per-gate overhead.