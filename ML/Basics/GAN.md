#ML 
Classical GANs feature $2$ major components:
1. **Generator**: [[Neural network]] that takes random noise (or a latent var) as input & outputs synthetic samples intended to mimic a target data distribution.
2. **Discriminator**: [[Neural network]] that receives samples from both the **real** dataset & the **generator**. It learns to classify if an input sample is “real                                                                                                                                                                                                                                  riminator identifies as| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------ |et 1. People are rational actors,<br>2. People are free from emotion or the effects of culture & social relations.<br>3. People are self-interested utility maximizers.<br>4. M& ts are efficient& irms are rational profit-maximizing organizations. ntropy** loss is $LD=−1|X|+|S|(∑x∈XlogD(x)+∑s∈Slog(1−D(G(s))))$ where $|X|$ and $|S|$ are the respective sizes of the sets.

The discriminator aims to **minimize** $LD$. Meanwhile, the generator $G$ must **fool** $D$. One way to formalize this is maximizing the discriminator's loss $−LD$. However, since $∑x∈XlogD(x)$ is const with respect to $G$, we can equivalently **minimize**: $L′G=1|S|∑s∈Slog(1−D(G(s)))$

In practice, a **$>$ stable** generator loss is often used: $LG=−1|S|∑s∈Slog(D(G(s)))$

Decreasing LG pushes the generator's outputs to be classified as real. At the ideal equilibrium, the discriminator outputs $D(x)≈D(G(s))≈12$ (because it cannot distinguish between real & generated data), leading to $LD=LG=−log12=log(2)≈0.6931$