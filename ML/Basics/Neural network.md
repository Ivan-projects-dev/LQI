#ML 
**Neural Networks** are based on how the human brain works:  
	Neurons are sending messages to each other, while the neurons are trying to solve a problem (over & over again), it is strengthening the connections that lead to success & diminishing the connections that lead to failure.
Neural Networks are **Multi-Layer Perceptrons**. In its simplest form, a neural network is made up from: **input layer, hidden layer, output layer**. In the **Neural Network Model**, input data are processed against a hidden layer before producing the final output.

**First Layer**: input perceptrons are making simple decisions based on the input, each single decision is sent to the perceptrons in the next layer.
**Second Layer**: inner perceptrons are making decisions by weighing the results from the first layer. This layer make $>$ complex decisions at a $>$ abstract level.
![[Pasted image 20251007200243.png]]
**Deep Neural Networks** are made up of several hidden layers of neural networks that perform complex operations on massive amounts of data. Each successive layer uses the preceding layer as input. For instance, optical reading uses low layers to identify edges, & higher layers to identify letters.