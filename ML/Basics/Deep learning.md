#ML 
**Deep neural networks** have a large num of layers with large num of nodes in each layer
**Deep learning** - training of deep [[Neural network]] on very large nums of training instances.
Each layer may be connected to previous layers in different ways 
	• Convolutional networks used for image processing 
	• $>$ complex architectures used for text processing, & machine translation, speech recognition, etc.