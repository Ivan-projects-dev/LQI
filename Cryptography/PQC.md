#Cybersecurity #Algorithm 
**Post-quantum cryptography (PQC)** replaces public-key algorithms whose security rests on factoring or discrete [[Logarithm]] problems - problems efficiently solved by [[Shor]]. The replacement algorithms are based on math problems that have no known efficient quantum algorithm.

Goal is **drop-in compatibility**: same roles (key exchange, digital signatures, encryption) filled by different math machinery underneath.